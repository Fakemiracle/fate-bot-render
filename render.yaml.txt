services:
  - type: web
    name: fate-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn bot:app -b 0.0.0.0:8000"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11